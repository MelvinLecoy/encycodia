import { DOMNode } from "@/typings";

const query = "_type == 'allofNextjs' && topic == 'Rendering'";
const title = 'SSG';

export default [
    {
        tag: 'h1',
        content: ['General Concepts']
    },
] as DOMNode[];